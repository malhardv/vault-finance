╔══════════════════════════════════════════════════════════════════╗
║                  VAULT - QUICK DEPLOYMENT GUIDE                  ║
╚══════════════════════════════════════════════════════════════════╝

🚀 EASIEST DEPLOYMENT PATH (Recommended for Beginners)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Setup MongoDB Database (5 minutes)
───────────────────────────────────────────
1. Go to https://www.mongodb.com/cloud/atlas
2. Sign up for free account
3. Create a FREE cluster (M0)
4. Create database user (save password!)
5. Allow access from anywhere (0.0.0.0/0)
6. Get connection string → Save it!

STEP 2: Deploy Backend to Render (10 minutes)
──────────────────────────────────────────────
1. Go to https://render.com and sign up
2. Click "New +" → "Web Service"
3. Connect your GitHub repository
4. Settings:
   - Name: vault-backend
   - Root Directory: backend
   - Build Command: npm install
   - Start Command: npm start
   - Instance Type: Free
5. Add Environment Variables:
   - PORT = 5000
   - NODE_ENV = production
   - MONGODB_URI = <your_mongodb_connection_string>
   - JWT_SECRET = <generate_random_string_here>
   - FRONTEND_URL = (leave empty for now)
6. Click "Create Web Service"
7. Wait for deployment (5-10 min)
8. Copy your backend URL (e.g., https://vault-backend.onrender.com)

STEP 3: Deploy Frontend to Vercel (5 minutes)
──────────────────────────────────────────────
1. Go to https://vercel.com and sign up
2. Click "Add New" → "Project"
3. Import your GitHub repository
4. Settings:
   - Framework Preset: Vite
   - Root Directory: frontend
   - Build Command: npm run build
   - Output Directory: dist
5. Add Environment Variable:
   - VITE_API_URL = <your_render_backend_url>
6. Click "Deploy"
7. Wait for deployment (2-3 min)
8. Copy your frontend URL (e.g., https://vault.vercel.app)

STEP 4: Update Backend CORS (2 minutes)
────────────────────────────────────────
1. Go back to Render dashboard
2. Select your backend service
3. Go to "Environment" tab
4. Update FRONTEND_URL = <your_vercel_frontend_url>
5. Click "Save Changes"
6. Backend will auto-redeploy

STEP 5: Seed Database (2 minutes)
──────────────────────────────────
1. In Render dashboard, go to your backend service
2. Click "Shell" tab (or use SSH)
3. Run: npm run seed
4. (Optional) Run: npm run seed:demo

STEP 6: Test Your App! 🎉
──────────────────────────
1. Visit your Vercel URL
2. Register a new account
3. Login and explore!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💰 COST: $0/month (100% FREE)
⏱️  TOTAL TIME: ~25 minutes
📚 DETAILED GUIDE: See DEPLOYMENT.md
✅ CHECKLIST: See DEPLOYMENT_CHECKLIST.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 ALTERNATIVE: Railway (All-in-One)
────────────────────────────────────
If you prefer deploying everything in one place:

1. Go to https://railway.app
2. Sign up with GitHub
3. Create new project from your repo
4. Railway auto-detects both frontend & backend
5. Add environment variables
6. Generate domains for both services
7. Done!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  IMPORTANT NOTES:
• Render free tier: Backend sleeps after 15 min of inactivity
• First request after sleep takes ~30 seconds to wake up
• MongoDB Atlas free tier: 512MB storage limit
• Keep your JWT_SECRET secure and random!
• Never commit .env files to git

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🆘 TROUBLESHOOTING:
• CORS errors? → Check FRONTEND_URL matches exactly
• Can't connect to DB? → Verify MongoDB connection string
• 404 errors? → Check API_URL in frontend env vars
• Backend not responding? → Check Render logs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📞 NEED HELP?
1. Check DEPLOYMENT.md for detailed instructions
2. Review logs on your deployment platform
3. Test backend health: https://your-backend-url/health

Good luck! 🚀
